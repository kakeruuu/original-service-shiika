<% if logged_in? %>
    <%= render 'posts/posts', posts: @posts %>
<% else %>
    <div class="d-flex" id="main">
        <%= image_tag 'logo.png', id: 'top-img', class: "align-items-center" %>
    </div>
<% end %>